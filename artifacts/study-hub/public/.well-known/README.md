# Well-known protocol endpoints

Study Hub does not currently publish MCP, A2A, UCP, ACP, API-catalog, agent-permissions, or agent-skills discovery documents. Requests for unimplemented protocol files should receive the site's normal 404 response.
