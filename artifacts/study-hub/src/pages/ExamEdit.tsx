import { useRef, useState } from "react";
import { useForm } from "react-hook-form";
import { useLocation, useRoute, useSearch } from "wouter";
import { ArrowLeft, BarChart2, Brain, Calendar, Clock3, FileQuestion, Pencil, Trash2, Upload } from "lucide-react";
import { useStudyData, type StudyType } from "@/hooks/useStudyData";
import { BottomSheet } from "@/components/shared/BottomSheet";
import { ConfirmSheet } from "@/components/shared/ConfirmSheet";
import { parseFinalExamFiles, type FinalExamImportError } from "@/lib/finalExamImport";

export function ExamEdit() {
  const [, params] = useRoute("/subjects/:subjectId/exams/:examId/edit"); const [, setLocation] = useLocation(); const search = useSearch();
  const { subjects, updateExam, schedulePlans, addSchedulePlan, updateSchedulePlan, deleteSchedulePlan } = useStudyData(); const subject = subjects.find(x => x.id === params?.subjectId); const exam = subject?.exams.find(x => x.id === params?.examId); const from = new URLSearchParams(search).get("from");
  const [questionsOpen, setQuestionsOpen] = useState(false), [confirmRemoveAll, setConfirmRemoveAll] = useState(false), [isImporting, setIsImporting] = useState(false), [errors, setErrors] = useState<FinalExamImportError[]>([]);
  const importRef = useRef<HTMLInputElement>(null), lastSavedRef = useRef("");
  const form = useForm({ defaultValues: { name: exam?.name ?? "", date: exam?.date ?? "", time: (exam as any)?.time ?? "09:00", weight: exam?.weight ?? 1 } });
  if (!subject || !exam) return <div className="p-8 text-center text-muted-foreground">Exam not found</div>;
  const flashKey = `studyhub:final-flashcards:${subject.id}:${exam.type}`; const flashcards = JSON.parse(localStorage.getItem(flashKey) ?? "[]"); const questionCount = exam.questions?.length ?? 0; const total = questionCount + flashcards.length;
  const inputCls = "min-w-0 w-full rounded-2xl border border-border/60 bg-secondary/40 px-4 py-3.5 text-sm text-foreground outline-none transition-all duration-200 focus:border-primary/40 focus:ring-2 focus:ring-primary/20";
  const goBack = () => from ? setLocation(decodeURIComponent(from)) : window.history.length > 1 ? window.history.back() : setLocation(`/subjects/${subject.id}/lectures?type=${exam.type}`);
  const importFiles = async (files: File[]) => { if (!files.length) return; setIsImporting(true); setErrors([]); try { const parsed = await parseFinalExamFiles(files); if (parsed.questions.length) updateExam(subject.id, exam.id, { questions: [...(exam.questions ?? []), ...parsed.questions] }); if (parsed.flashcards.length) localStorage.setItem(flashKey, JSON.stringify([...flashcards, ...parsed.flashcards])); setErrors(parsed.errors); setQuestionsOpen(false); } finally { setIsImporting(false); } };
  const removeAll = () => { updateExam(subject.id, exam.id, { questions: [], lastScore: null, checked: false }); localStorage.setItem(flashKey, "[]"); setConfirmRemoveAll(false); setQuestionsOpen(false); };
  const onSave = (data: any) => { const date = data.date || null, time = data.time || "09:00", name = data.name.trim() || exam.name, weight = parseFloat(data.weight) || 1; const signature = JSON.stringify({ name, date, time, weight }); if (lastSavedRef.current === signature) return; lastSavedRef.current = signature; updateExam(subject.id, exam.id, { name, date, weight, time } as any); const linkedPlan = schedulePlans.find(plan => plan.type === "exam" && (plan as any).source === "finalExam" && plan.items.some(item => (item as any).examId === exam.id)); if (!date) { if (linkedPlan) deleteSchedulePlan(linkedPlan.id); return; } const existingItem = linkedPlan?.items.find(item => (item as any).examId === exam.id); const item = { id: existingItem?.id ?? crypto.randomUUID(), subjectName: `${subject.name} · ${name}`, subjectId: subject.id, date, time, checked: existingItem?.checked ?? false, examId: exam.id } as any; const plan = { type: "exam" as const, title: name, startDate: date, endDate: date, importance: linkedPlan?.importance ?? 2, source: "finalExam", items: [item] } as any; if (linkedPlan) updateSchedulePlan(linkedPlan.id, plan); else addSchedulePlan(plan); };
  const editPath = `/subjects/${subject.id}/exams/${exam.id}/questions?from=${encodeURIComponent(`${window.location.pathname}${window.location.search}`)}`;
  return <div className="space-y-5 pb-24"><div className="relative -mx-4 border-b border-border/30 bg-gradient-to-b from-primary/10 via-primary/5 to-transparent px-4 pb-6 pt-2 md:-mx-6 md:px-6"><div className="flex items-start gap-3"><button onClick={goBack} className="mt-1 flex h-10 w-10 items-center justify-center rounded-full border border-border/50 bg-background/80 shadow-sm"><ArrowLeft className="h-4 w-4" /></button><div className="min-w-0 flex-1 py-1"><p className="mb-1 text-xs font-semibold uppercase tracking-widest text-primary/75">{subject.name} · Exam</p><h1 className="text-2xl font-bold">{exam.name}</h1><p className="mt-2 text-xs text-muted-foreground">{questionCount} MCQs · {flashcards.length} flashcards</p></div></div></div><div className="grid grid-cols-2 gap-3"><button onClick={() => setQuestionsOpen(true)} className="group rounded-2xl border border-primary/20 bg-primary/10 p-4 text-left"><FileQuestion className="mb-3 h-5 w-5 text-primary" /><p className="text-sm font-bold">Questions</p><p className="text-[11px] text-muted-foreground">{total ? `${total} total items` : "Add exam content"}</p></button><button disabled={!total} onClick={() => setLocation(`/subjects/${subject.id}/exams/${exam.id}/take`)} className="rounded-2xl border border-primary/20 bg-primary/10 p-4 text-left disabled:opacity-40"><Brain className="mb-3 h-5 w-5 text-primary" /><p className="text-sm font-bold">Take Exam</p><p className="text-[11px] text-muted-foreground">Start unified session</p></button></div>{errors.length > 0 && <div className="rounded-xl border border-destructive/20 bg-destructive/10 px-4 py-3 text-sm text-destructive">{errors.map(x => <p key={x.filename}>{x.filename}: {x.reason}</p>)}</div>}<form onSubmit={form.handleSubmit(onSave)} onBlur={form.handleSubmit(onSave)}><div className="space-y-4 rounded-2xl border border-border/50 bg-card p-4 shadow-sm"><label className="block text-xs font-bold uppercase tracking-widest text-muted-foreground">Details</label><input {...form.register("name", { required: true })} className={inputCls} /><div className="grid grid-cols-2 gap-3"><div className="min-w-0"><label className="mb-1 block text-xs text-muted-foreground"><Calendar className="mr-1 inline h-3 w-3" />Date</label><input type="date" {...form.register("date")} className={inputCls} /></div><div className="min-w-0"><label className="mb-1 block text-xs text-muted-foreground"><BarChart2 className="mr-1 inline h-3 w-3" />Weight %</label><input type="number" min="0" max="100" {...form.register("weight")} className={inputCls} /></div></div><div className="min-w-0"><label className="mb-1 block text-xs text-muted-foreground"><Clock3 className="mr-1 inline h-3 w-3" />Time</label><input type="time" {...form.register("time")} className={inputCls} /></div></div></form><div className="rounded-2xl border border-border/50 bg-card p-4"><label className="mb-3 block text-xs font-bold uppercase tracking-widest text-muted-foreground">Type</label><div className="flex gap-1 rounded-xl bg-secondary/50 p-1">{(["theoretical", "practical"] as StudyType[]).map(type => <button key={type} onClick={() => updateExam(subject.id, exam.id, { type })} className={`flex-1 rounded-xl py-2.5 font-semibold capitalize ${exam.type === type ? "bg-background shadow-sm" : "text-muted-foreground"}`}>{type}</button>)}</div></div><BottomSheet isOpen={questionsOpen} onClose={() => setQuestionsOpen(false)} title="Questions"><div className="space-y-2.5"><button onClick={() => importRef.current?.click()} disabled={isImporting} className="flex w-full items-center gap-4 rounded-2xl bg-secondary/50 p-4 text-left disabled:opacity-50"><Upload className="text-primary" /><span>Add questions</span></button><button onClick={() => setLocation(editPath)} className="flex w-full items-center gap-4 rounded-2xl bg-secondary/50 p-4 text-left"><Pencil className="text-primary" /><span>Edit questions</span></button><button onClick={() => setConfirmRemoveAll(true)} className="flex w-full items-center gap-4 rounded-2xl bg-destructive/10 p-4 text-left text-destructive"><Trash2 /><span>Remove all questions</span></button></div></BottomSheet><input ref={importRef} type="file" accept=".xlsx,.xls" multiple className="hidden" onChange={e => { importFiles(Array.from(e.target.files ?? [])); e.target.value = ""; }} /><ConfirmSheet isOpen={confirmRemoveAll} onClose={() => setConfirmRemoveAll(false)} onConfirm={removeAll} title="Remove all questions?" message="This clears every Final Exam item." confirmLabel="Remove All Questions" /></div>;
}
